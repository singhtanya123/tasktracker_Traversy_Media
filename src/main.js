import { createApp } from 'vue'
import App from './App.vue'

/* In this createApp will initalize and create then it will contain 
data/options/objects / so here we are passing file name App.
and mount div id #app*/
createApp(App).mount('#app')
